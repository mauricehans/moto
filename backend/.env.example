# ========================================
# CONFIGURATION DE SÉCURITÉ - AGDE MOTO
# ========================================
# ⚠️  IMPORTANT: Utilisez le script scripts/generate_secrets.py pour générer des valeurs sécurisées
# ⚠️  CRITIQUE: Changez TOUTES les valeurs par défaut avant la mise en production

# Django Settings - CRITIQUE POUR LA SÉCURITÉ
# Générez avec: python scripts/generate_secrets.py (option 1)
SECRET_KEY=CHANGEZ-CETTE-CLE-SECRETE-DJANGO-50-CARACTERES-MINIMUM-OBLIGATOIRE
DEBUG=False
# Remplacez yourdomain.com par votre domaine réel
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# Configuration HTTPS - OBLIGATOIRE EN PRODUCTION
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True
SECURE_CONTENT_TYPE_NOSNIFF=True
SECURE_BROWSER_XSS_FILTER=True
X_FRAME_OPTIONS=DENY

# Database (PostgreSQL recommandé pour la production)
DB_NAME=agde_moto
DB_USER=agde_moto_user
# Générez avec: python scripts/generate_secrets.py (option 2) - 32+ caractères
DB_PASSWORD=CHANGEZ-CE-MOT-DE-PASSE-BASE-DE-DONNEES-32-CARACTERES-MINIMUM
DB_HOST=localhost
DB_PORT=5432

# Cache (Redis pour la production)
# Générez avec: python scripts/generate_secrets.py (option 4)
REDIS_URL=redis://:CHANGEZ-MOT-DE-PASSE-REDIS@127.0.0.1:6379/1
REDIS_PASSWORD=CHANGEZ-MOT-DE-PASSE-REDIS-32-CARACTERES

# Configuration JWT - SÉCURITÉ RENFORCÉE
# Générez avec: python scripts/generate_secrets.py (option 3)
JWT_SECRET_KEY=CHANGEZ-CETTE-CLE-JWT-64-CARACTERES-MINIMUM-OBLIGATOIRE
JWT_ACCESS_TOKEN_LIFETIME=15
JWT_REFRESH_TOKEN_LIFETIME=1440

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@agdemoto.com
SERVER_EMAIL=admin@agdemoto.com

# CORS - RESTRICTIF EN PRODUCTION
# Remplacez par vos domaines réels, supprimez localhost en production
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3002,https://yourdomain.com
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOW_CREDENTIALS=True

# File uploads - Sécurité renforcée
MAX_UPLOAD_SIZE=10485760  # 10MB
FILE_UPLOAD_MAX_MEMORY_SIZE=2621440  # 2.5MB
DATA_UPLOAD_MAX_MEMORY_SIZE=2621440  # 2.5MB

# Session Security - CRITIQUE POUR LA PRODUCTION
SESSION_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Strict
SESSION_COOKIE_AGE=3600  # 1 heure
CSRF_COOKIE_SECURE=True
CSRF_COOKIE_HTTPONLY=True
CSRF_COOKIE_SAMESITE=Strict

# Logging et Monitoring
LOGGING_LEVEL=INFO
LOG_SQL_QUERIES=False
ENABLE_SECURITY_LOGGING=True

# Rate Limiting (optionnel avec django-ratelimit)
RATE_LIMIT_ENABLE=True
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# ========================================
# INSTRUCTIONS DE SÉCURITÉ CRITIQUES
# ========================================
# 🚨 AVANT LA MISE EN PRODUCTION - OBLIGATOIRE:
# 1. Exécutez: python scripts/generate_secrets.py
# 2. Remplacez TOUTES les valeurs CHANGEZ-* ci-dessus
# 3. Définissez DEBUG=False (déjà fait)
# 4. Configurez ALLOWED_HOSTS avec vos domaines réels uniquement
# 5. Activez HTTPS avec un certificat SSL valide
# 6. Utilisez PostgreSQL (pas SQLite) en production
# 7. Sécurisez Redis avec un mot de passe fort
# 8. Limitez CORS_ALLOWED_ORIGINS aux domaines autorisés uniquement
# 9. Configurez un système de monitoring et d'alertes
# 10. Planifiez la rotation des mots de passe (90 jours)
#
# 📖 DOCUMENTATION COMPLÈTE:
# Consultez scripts/SECURITY_GUIDE.md pour le guide détaillé
#
# 🔧 OUTILS DE VÉRIFICATION:
# - python manage.py check --deploy
# - python manage.py check --tag security
#
# ⚠️  EN CAS DE PROBLÈME:
# Consultez la section "En cas de compromission" dans SECURITY_GUIDE.md